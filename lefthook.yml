pre-commit:
  parallel: true
  commands:
    lint-staged:
      run: npx lint-staged
    typecheck:
      run: npx tsc --noEmit

commit-msg:
  commands:
    commitlint:
      run: npx --no-install commitlint --edit {1}
