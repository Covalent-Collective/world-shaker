# World Shaker — UX v1 (v0 MVP) Implementation Plan — v2

- **Plan ID**: world-shaker-ux-v1
- **Source spec**: `/Users/jyong/projects/world-shaker/.omc/specs/deep-interview-world-shaker-ux-v1.md` (ambiguity 4.5%, 19 rounds)
- **Branch**: `spec/deep-interview-ux-v1`
- **Mode**: RALPLAN-DR consensus, SHORT deliberation (no `--deliberate` flag set)
- **Target**: ship v0 MVP that delivers the S-grade conversation chemistry experience end-to-end in ~4-6 weeks
- **Generated**: 2026-04-26 by `oh-my-claudecode:planner` (revised: v2 — iteration 2/5)

---

## RALPLAN-DR Summary (for Architect/Critic alignment)

### Principles (5)

1. **S-grade conversation chemistry first.** Every cut, defer, or scope reduction must protect the agent-to-agent dialogue craft. Visuals, world fidelity, animation, push polish — all subordinate.
2. **Brownfield discipline.** Existing schema, RLS, JWT auth, OpenRouter gateway, and Inngest scaffold (`/Users/jyong/projects/world-shaker/lib/`, `/Users/jyong/projects/world-shaker/supabase/migrations/`) are load-bearing. Additive migrations only; no rewrites of `0001_initial.sql` / `0002_rls.sql`.
3. **Resumable + observable.** Every long-running surface (interview, live conversation, avatar generation, first-encounter pipeline) persists state per turn so a dropped session resumes; PostHog (already wired in `/Users/jyong/projects/world-shaker/lib/posthog/`) carries the moat data layer.
4. **Quiet protector tone.** Calm, explanation-first, no arcade gamification. Worldcoin Verified-Human badge surfaces on every relevant screen but never as a banner.
5. **Defer to v1+ aggressively when it does not erode the S-grade.** Sims-style isometric world, Track B proactive encounters, and World Chat first-message coaching cosmetics can wait. KR+EN bilingual schema/i18n shell ships in v0; **EN prompt rendering is gated behind `BILINGUAL_PROMPTS_V1` flag and deferred to Phase 5** so Phase 2's S-grade rubric pass runs against KR-only first.

### Decision Drivers (top 3)

1. **Live streaming infra cost vs. story integrity.** OpenRouter streaming via Inngest with client SSE relay is more complex than a pre-rendered transcript. Driver: streaming is what makes "watching the date" feel alive — non-negotiable for S-grade. Trade: must engineer back-pressure, abandon, and resume semantics from day one.
2. **Track A vs. dual-track scope.** Spec mandates dual-track (blind nightly + Pokemon proactive). Driver: 4-6 week budget can land Track A end-to-end with polish, or both tracks at half-quality. Choosing Track A end-to-end (with Track B as a hidden feature flag stub) protects S-grade chemistry.
3. **KR+EN bilingual at launch vs. KR-only fast lane.** Synthesized: ship Option A schema+i18n shell in Phase 1, evaluate KR-only in Phase 2 against rubric (single pass), add EN prompt rendering in Phase 5 behind a flag and re-run rubric in EN before launch. Avoids the re-engineering loop while protecting Phase 2 S-grade focus.

### Viable Options

#### Option A — **Track A end-to-end + Track B feature-flag stub + bilingual schema/shell now, EN prompts in Phase 5** (RECOMMENDED)

- **Pros**: Ships the S-grade loop (interview → first encounter → live conversation → report → like → mutual → World Chat) at full polish in 4-6 weeks. Track B can be enabled progressively post-launch via a single flag without schema rework. Bilingual schema and i18n shell wired in Phase 1; EN prompt rendering deferred behind `BILINGUAL_PROMPTS_V1` flag so Phase 2 rubric runs KR-only.
- **Cons**: Day-1 demo lacks the Pokemon "stroll" wow factor; product owner may push back on the visible defer. EN launch posture is conditional on Phase 5 EN rubric pass — small launch-date risk.

#### Option B — **Both tracks half-quality at launch**

- **Pros**: Demonstrates the full vision; matches spec literally.
- **Cons**: 4-6 week budget forces shortcuts on Track A polish (compressed prompt iteration, no first-encounter immediacy, weak failure UX). Erodes the S-grade. Track B world surface lands as a placeholder grid that ships worse than not shipping it.

#### Option C — **Track A only, KR-only schema, bilingual deferred entirely**

- **Pros**: Fastest path to a single-language polished demo. Saves ~3-5 days.
- **Cons**: Re-engineering bilingual prompts and i18n routing post-launch is more expensive than wiring it in now (every persona/dialogue/report prompt needs a `language` argument). Spec resolution R15 explicitly rejected KR-only. Tension surfaced and called out.

**Selected: Option A.** Tension with spec on Track B fidelity is acknowledged; mitigation is shipping a non-empty "Daily Stroll cards" surface that gestures at Track B without committing to the Sims-style world. Bilingual scope is preserved at the schema/shell layer; EN prompt rendering is sequenced into Phase 5 with its own rubric gate.

If only Option A survives Critic review: Options B/C are invalidated by (B) S-grade erosion, (C) explicit R15 spec resolution.

---

## Requirements Summary (drawn from spec)

The v0 MVP must deliver these flows:

1. **Onboarding**: Orb verify → 30s narrative video (skip after 5s) → hybrid interview (5-6 skeleton questions + 1-2 LLM probes each) → avatar generation → first encounter spawn — all within ≤10 minutes.
2. **First Encounter (Day 1 immediate)**: Within 60s of interview completion, the user can watch a live agent-to-agent conversation. Alpha-stage seed-agent fallback prevents empty pool. **Pipeline is recoverable** — if the user drops off mid-spawn, on next app load the server detects orphaned `agent_status='active'` with no conversation row and re-emits `agent.activated`.
3. **Live Conversation Streaming**: OpenRouter streaming → Inngest orchestration → server-sent events to client. Both agents speak with distinct personas. 12-25 turns. User can leave; conversation finishes in background; report ready on return. **SSE supports `Last-Event-ID` reconnect** so dropped connections resume from the last delivered turn rather than restarting the stream.
4. **Report (Match Card)**: Curated highlights consumable in ≤30s (default view); "전문 보기" toggle reveals full transcript. `compatibility_score`, `why_click`, `watch_out`, `highlight_quotes`, `rendered_transcript` all populated. `compatibility_score` computed by `match_candidates()` SQL function combining pgvector cosine similarity + structured-feature delta.
5. **Track A (Blind Discover)**: Existing nightly Inngest job (`/Users/jyong/projects/world-shaker/lib/inngest/functions/nightly-match.ts`) generates encounters; reports surface in daily digest.
6. **Track B (Proactive)**: v0 ships **"Daily Stroll cards"** — a curated horizontal card-stack of NPC agents (random + people who liked you), each tap triggers an encounter. Sims-style isometric world is v1+ aspiration, gated behind a `STROLL_WORLD_V1` feature flag.
7. **Like Mechanic**: 좋아요 / 스킵 on report. Mutual = match-success screen with 2 first-message starters + one-tap World Chat handoff.
8. **Daily Quota**: 3-5 encounters/day enforced via outcome_events count; non-coercive "내일 다시" copy.
9. **Notifications**: Daily digest push (morning) + impact-only push on mutual match. World App push capability uncertain — graceful in-app badge fallback.
10. **Safety**: Roll-up menu on match/conversation/chat screens. Existing 2-strike auto-suspend trigger reused. Rate-limiting on `/api/agent/answer` prevents chat-flood DoS.
11. **Language (KR+EN)**: Auto-detect from World App locale; user-switchable. UI strings + schema are language-aware in Phase 1; EN prompts behind `BILINGUAL_PROMPTS_V1` flag in Phase 5.
12. **Living Agent**: Edit answers, request more questions, paste context dump. Each path triggers `extracted_features` re-derivation + embedding re-compute.
13. **Failure Handling**: LLM timeout / repeat-loop / NSFW moderation / hostile-tone heuristic → [대화 재시작] [대화 닫기] overlay within 5s. No silent retry, no infinite spinner.
14. **Verified Human Badge**: Discreet World ID Orb badge on every profile/report/match screen.
15. **Cost-cap kill switch**: Daily LLM-spend ceiling triggers automatic streaming pause + user-facing "오늘은 산책이 모두 마감되었어요".

---

## Acceptance Criteria (testable, file-level concrete)

| #     | Criterion                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Verification                                                                                                                                                                                                                                                |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| AC-1  | `app/(onboarding)/verify/page.tsx` calls `/api/verify` and on success routes to `/onboarding/intro` (30s video).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | E2E Playwright: stub IDKit success → expect navigation.                                                                                                                                                                                                     |
| AC-2  | `app/(onboarding)/intro/page.tsx` plays 30s video, skip CTA appears after 5s, completion fires `posthog.capture('onboarding_video_completed')`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Vitest jsdom + fake timers.                                                                                                                                                                                                                                 |
| AC-3  | `app/(onboarding)/interview/page.tsx` renders 5-6 fixed skeleton questions; each answer POST to `/api/agent/answer` persists immediately into `agents.interview_answers`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Integration: post answer → query Supabase service client → assert key present.                                                                                                                                                                              |
| AC-4  | After last skeleton answer, the LLM probe prompt (`lib/llm/prompts/interview-probe.ts`) generates 1-2 follow-ups streamed back; user answer persists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Mock OpenRouter; assert request shape; assert DB write.                                                                                                                                                                                                     |
| AC-5  | Interview completion triggers Inngest event `agent.activated` which spawns `firstEncounter` function in `lib/inngest/functions/first-encounter.ts` within 60s.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Inngest dev runner + fake clock.                                                                                                                                                                                                                            |
| AC-6  | `lib/inngest/functions/live-conversation.ts` streams turns via OpenRouter streaming; persists each turn to `conversations.turns` and emits Inngest progress events. Every turn append uses `WHERE status = 'live'` predicate; zero-rows-affected aborts the run cleanly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Integration: assert N turns appended in order with monotonic indices; assert abandoned conversation halts append.                                                                                                                                           |
| AC-7  | `app/(app)/conversation/[id]/page.tsx` renders turns live via SSE (`app/api/conversation/[id]/stream/route.ts`); user navigating away and returning sees the full transcript when `conversations.status='completed'`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Playwright: open page → close → reopen → expect full transcript.                                                                                                                                                                                            |
| AC-7b | **SSE resume protocol**: server emits `id: <turn_index>` per SSE event; client sends `Last-Event-ID` on reconnect; route resumes streaming from `turns[Last-Event-ID + 1]` onwards. Supports mid-session drop without restart.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Integration: open SSE → drop after turn 5 → reconnect with `Last-Event-ID: 5` → assert turns 6+ delivered, no duplicates.                                                                                                                                   |
| AC-8  | On `conversations.status='completed'` the post-process step generates the report (`lib/llm/prompts/report.ts`) and writes `matches.compatibility_score`, `why_click`, `watch_out`, `highlight_quotes`, `rendered_transcript`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Integration: assert columns non-null.                                                                                                                                                                                                                       |
| AC-8b | **`match_candidates()` SQL function** in `0006_compatibility_score.sql` returns top-K candidates ranked by `0.6 * (1 - cosine_distance(emb_a, emb_b)) + 0.4 * structured_feature_score(features_a, features_b)`. Function is `STABLE`, callable from service-role only, takes `(target_user uuid, k int)` and returns `(candidate_user uuid, score real)`. **Exclusion semantics enforced inside SQL body**: `WHERE candidate.user_id <> target_user AND candidate.status = 'active' AND NOT EXISTS (SELECT 1 FROM matches m WHERE ((m.user_id = target_user AND m.candidate_user_id = candidate.user_id) OR (m.user_id = candidate.user_id AND m.candidate_user_id = target_user)) AND m.status IN ('pending','accepted','mutual'))`. Weights `0.6/0.4` are uncalibrated v0 defaults — flagged in migration header as `# TODO: calibrate post-launch via outcome_events feedback`. | Unit test: insert 50 fixture agents incl. 1 self + 1 pre-existing pending match → call function → assert ordering monotonic, scores in `[0,1]`, both self and already-matched candidates excluded from results.                                             |
| AC-9  | `app/(app)/match/[id]/page.tsx` default view shows highlights consumable in ≤30s; `<Toggle>` reveals full `rendered_transcript`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Visual snapshot + word-count assertion (target ≤120 words highlights).                                                                                                                                                                                      |
| AC-10 | 좋아요 button calls `/api/match/[id]/like` (next-safe-action server action) → updates `matches.status='accepted'`; mutual detection via the **revised partial UNIQUE** `(user_id, candidate_user_id, origin) WHERE status IN ('pending','accepted','mutual')`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | RLS test: only owner can update; status transitions allowed only `pending → accepted/skipped`.                                                                                                                                                              |
| AC-11 | Mutual match → `app/(app)/match/[id]/success/page.tsx` renders 2 first-message starters (from `lib/llm/prompts/first-message.ts`) + tappable World Chat link. Starters pre-generated at `match.created` time and cached on the match row to keep success-render <500ms.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | E2E: simulate both sides accept → expect success route + `world_chat_link` set.                                                                                                                                                                             |
| AC-12 | Daily quota: `lib/quota/daily.ts` reads `outcome_events` of type `viewed` for current `users.timezone`-local day; 4th request returns "tomorrow" UI on `app/(app)/stroll/page.tsx`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Integration: seed 3 events → 4th attempt blocked.                                                                                                                                                                                                           |
| AC-13 | `app/(app)/stroll/page.tsx` renders Daily Stroll card stack (random NPC pool + incoming-like seeds); tap fires encounter spawn. Candidates ranked by `match_candidates()` from AC-8b.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | RLS: only includes `agent_status='active'` candidates not already in pending match.                                                                                                                                                                         |
| AC-14 | KR+EN: `lib/i18n/messages.ts` exports KR/EN message catalogs; user setting at `users.language_pref` persists via `/api/user/language`; LLM prompts accept `language` directive. **EN prompt rendering gated behind `BILINGUAL_PROMPTS_V1` flag**; until flag is on, prompts force KR.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Vitest: render component with each locale; assert prompts include language line; assert flag gate on prompt selection.                                                                                                                                      |
| AC-15 | Living Agent: `app/(app)/settings/agent/page.tsx` allows editing existing answers; submit triggers `agent.updated` Inngest event → re-extracts features → re-embeds.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Integration: edit answer → assert new `embedding` vector + new `extracted_features` snapshot.                                                                                                                                                               |
| AC-16 | Conversation failure: server detects timeout/repeat-loop/NSFW/hostile → emits `conversation.failed` → sets `conversations.status='failed'` → client overlay [대화 재시작] [대화 닫기] visible within 5s of failure event.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Inject fake LLM failure → assert overlay DOM + outcome_event written + status persisted.                                                                                                                                                                    |
| AC-17 | Avatar pipeline: `lib/avatar/generate.ts` (called from `firstEncounter` Inngest fn) generates stylized avatar via OpenRouter image model, writes URL to `agents.avatar_url`, completes within 60s of interview. **Image-model availability is a Phase 3 blocking pre-condition**: if no model is provisioned by Phase 3 kickoff, fall back to deterministic placeholder set keyed on hash of features and document the gap.                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Integration: assert `agents.avatar_url` non-null within deadline; pre-condition check in CI.                                                                                                                                                                |
| AC-18 | Verified Human badge: `components/world/VerifiedHumanBadge.tsx` rendered on `/match/[id]`, `/conversation/[id]`, `/profile`, `/match/[id]/success`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Component test: assert presence + `aria-label='World ID Verified Human'`.                                                                                                                                                                                   |
| AC-19 | PostHog event coverage: events `interview_started`, `interview_completed`, `first_encounter_spawned`, `conversation_streaming_started`, `conversation_completed`, `report_viewed`, `report_expanded`, `like_sent`, `mutual_match`, `world_chat_opened`, `quota_blocked`, `conversation_failed_overlay_shown`, `llm_cost`, `streaming_paused_cost_cap` fire with \*\*`distinct_id = users.posthog_cohort = sha256(world_user_id                                                                                                                                                                                                                                                                                                                                                                                                                                                      |                                                                                                                                                                                                                                                             | rotation_salt)`**, salt rotated quarterly via `lib/posthog/cohort.ts`. Raw `world_user_id`never leaves the server. **Dashboard continuity**: every event also includes`posthog_cohort_predecessor` person property = previous quarter's cohort hash (so retention/funnels can be bridged across rotation boundaries via SQL JOIN on the predecessor chain). Rotation Inngest fn writes the predecessor before flipping the salt. | PostHog mock + `expect(capture).toHaveBeenCalledWith({ distinct_id: matches(/^[0-9a-f]{64}$/), $set: { posthog_cohort_predecessor: matches(/^[0-9a-f]{64}$/ | null) } })`. |
| AC-20 | All client routes that read user-scoped tables go through `getServerClient()` from `/Users/jyong/projects/world-shaker/lib/supabase/server.ts` so RLS via `current_world_user_id()` applies. **`getServiceClient()` whitelist**: `/api/inngest`, `/api/verify`, `/api/wallet-auth`, `/api/conversation/[id]/stream/route.ts` (documented reason: `conversations` table is service-role-only per `0002_rls.sql:38-41`; SSE relay must read turns server-side and shape them for the client), and Inngest functions. **CI grep guard** at `tests/ci/service-client-whitelist.test.ts`: `grep -rln "getServiceClient(" lib app                                                                                                                                                                                                                                                         | sort > /tmp/callers && diff /tmp/callers .omc/plans/service-client-allowlist.txt`— fails CI on any new caller not on the allowlist. Whitelist documented in`lib/supabase/server.ts`header comment AND mirrored in`.omc/plans/service-client-allowlist.txt`. | CI grep test runs on every PR; whitelist drift fails the build.                                                                                                                                                                                                                                                                                                                                                                  |
| AC-21 | **First-encounter pipeline recovery**: on app load, if `agents.status='active'` AND no `conversations` row exists for this user's agent, server triggers `agent.activated` re-emission and renders "preparing your first encounter" placeholder on `app/(app)/page.tsx`. Recovery is idempotent — repeated loads do not multi-spawn.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Integration: insert active agent with no conversation → load home → assert event re-emitted exactly once + placeholder visible.                                                                                                                             |
| AC-22 | **Rate limiting on `/api/agent/answer`**: 30 requests/min per `world_user_id` via `next-safe-action` middleware in `lib/auth/rate-limit.ts`; 31st returns HTTP 429 with `Retry-After` header; PostHog `rate_limit_hit` event fires.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Integration: 31 sequential POSTs → assert last is 429 + event captured.                                                                                                                                                                                     |
| AC-23 | **Cost-cap kill switch (two-tier)**: Inngest scheduled job `lib/inngest/functions/cost-cap.ts` runs daily at 00:01 UTC (settles yesterday) AND a **mid-day probe** every 4 hours (04/08/12/16/20 UTC) that aggregates the in-progress UTC day's `llm_cost` events; if cumulative `> app_settings.cost_cap_usd_daily`, sets `app_settings.streaming_paused = true` defensively (narrows worst-case burn window from ~24h to ~4h). Stroll surface and conversation spawn paths read this flag; when paused, render "오늘은 산책이 모두 마감되었어요" copy and refuse encounter spawn. Manual override via service-role row update; flag auto-resets on the 00:01 settlement run.                                                                                                                                                                                                      | Integration: (a) settlement: seed cost events over threshold → run daily job → assert flag set + spawn blocked + copy rendered; (b) probe: seed cost events at 02:00 UTC over threshold → run 04:00 probe → assert flag set within the same UTC day.        |

---

## Implementation Steps

> **Effort scale**: S = ≤1 day, M = 2-3 days, L = 4-7 days. Total target: 26-39 person-days for solo, parallelizable to 19-26 elapsed days with 2 specialists (one on prompts/streaming, one on UX surfaces).

### Phase 1 — Foundation (schema + auth + i18n shell + ranking fn)

**Goal**: load-bearing infra additions that everything else depends on.

| Step                                                      | Files (create / modify)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Effort |
| --------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| 1.1 Schema migration `0003_ux_v1.sql`                     | **CREATE** `/Users/jyong/projects/world-shaker/supabase/migrations/0003_ux_v1.sql`. Additions: `agents.avatar_url TEXT`, `agents.avatar_generated_at TIMESTAMPTZ`, `agents.language_pref TEXT CHECK (language_pref IN ('ko','en'))`, `agents.is_seed BOOLEAN NOT NULL DEFAULT FALSE`, `agents.growth_log JSONB NOT NULL DEFAULT '[]'::jsonb`, `users.language_pref TEXT NOT NULL DEFAULT 'ko'`, `users.timezone TEXT NOT NULL DEFAULT 'Asia/Seoul'`, `users.posthog_cohort TEXT` (populated via trigger from `sha256(id                                                                                                                  |        | current_salt)`), `matches.origin TEXT NOT NULL DEFAULT 'system_generated' CHECK (origin IN ('system_generated','user_initiated_proactive','encounter_spawned'))`, `matches.starters JSONB`(pre-generated first-message cache). **Conversations status state machine**:`ALTER TABLE conversations ADD COLUMN status TEXT NOT NULL DEFAULT 'completed' CHECK (status IN ('live','completed','abandoned','failed'))`(backfill existing rows as`completed`), then `ALTER COLUMN status SET DEFAULT 'live'`for new rows. Add`conversations.last_turn_emitted_at TIMESTAMPTZ`for SSE resume bookkeeping. Add`BEFORE UPDATE`trigger`conversations_status_transition_guard`enforcing valid transitions:`live → completed/abandoned/failed`; terminal states are immutable. Drop+recreate matches partial UNIQUE: `DROP INDEX matches_user_candidate_active_unique; CREATE UNIQUE INDEX matches_user_candidate_origin_active_unique ON public.matches (user_id, candidate_user_id, origin) WHERE status IN ('pending','accepted','mutual')`— documented tradeoff: same pair can have one match per origin track simultaneously (intentional for dual-track). Mirror in`/Users/jyong/projects/world-shaker/types/db.ts`. | M   |
| 1.2 RLS additions `0004_rls_ux_v1.sql`                    | **CREATE** `/Users/jyong/projects/world-shaker/supabase/migrations/0004_rls_ux_v1.sql`. Policies: `users_update_own_language` (users may UPDATE only `language_pref`/`timezone` columns on own row); `agents_update_own_growth` (agent owner may UPDATE `avatar_url`/`growth_log`/`language_pref`). **`conversations` stays service-role-only** — `/api/conversation/[id]/stream/route.ts` is added to the `getServiceClient()` whitelist (Step 1.7) rather than opening a client-readable RLS policy. Rationale: keeps the existing hard-block contract intact and minimizes attack surface; SSE route shapes the data the client sees. | S      |
| 1.3 Settings table `0005_app_settings.sql`                | **CREATE** `/Users/jyong/projects/world-shaker/supabase/migrations/0005_app_settings.sql`. Single-row `app_settings` table: `streaming_paused BOOLEAN NOT NULL DEFAULT FALSE`, `cost_cap_usd_daily NUMERIC(10,2) NOT NULL DEFAULT 50.00`, `posthog_cohort_salt TEXT NOT NULL`, `posthog_cohort_salt_rotated_at TIMESTAMPTZ NOT NULL DEFAULT now()`. Service-role-only. Used by AC-23 cost cap and AC-19 cohort hashing.                                                                                                                                                                                                                  | S      |
| 1.4 Compatibility score fn `0006_compatibility_score.sql` | **CREATE** `/Users/jyong/projects/world-shaker/supabase/migrations/0006_compatibility_score.sql`. Implements `match_candidates(target_user uuid, k int) RETURNS TABLE(candidate_user uuid, score real)`. Body: `0.6 * (1 - (target.embedding <=> c.embedding)) + 0.4 * structured_feature_score(target.extracted_features, c.extracted_features)` where `structured_feature_score` is a helper SQL function over JSONB (penalty for direct contradictions on age-band/values/lifestyle keys; bonus for shared interests). `STABLE`, executes as `SECURITY DEFINER`, granted to service-role only.                                        | M      |
| 1.5 Rollback companion `0003_rollback.sql`                | **CREATE** `/Users/jyong/projects/world-shaker/supabase/migrations/0003_rollback.sql` with reverse migrations (DROP COLUMN, DROP INDEX, DROP FUNCTION) for migrations 0003-0006. Header comment: `-- DO NOT auto-run. Manual recovery only. Run with explicit operator approval after taking a DB snapshot.`                                                                                                                                                                                                                                                                                                                             | S      |
| 1.6 i18n shell                                            | **CREATE** `/Users/jyong/projects/world-shaker/lib/i18n/messages.ts` (KR/EN flat dictionaries), `/Users/jyong/projects/world-shaker/lib/i18n/useT.ts` (client hook), `/Users/jyong/projects/world-shaker/lib/i18n/getT.ts` (server). Wire `lang` cookie + `users.language_pref` round-trip via `/api/user/language` (next-safe-action). Modify `app/layout.tsx` to inject `lang` based on cookie. Add `BILINGUAL_PROMPTS_V1` flag in `lib/flags.ts` defaulting to `false` — controls whether prompts render EN.                                                                                                                          | M      |
| 1.7 Auth + service-client whitelist refresh               | Modify `/Users/jyong/projects/world-shaker/lib/auth/jwt.ts` to attach `language_pref` claim. Modify `/Users/jyong/projects/world-shaker/app/api/verify/route.ts` to derive language from request `Accept-Language` and persist on user creation, plus populate `users.timezone` from MiniKit context (UTC fallback). Update `/Users/jyong/projects/world-shaker/lib/supabase/server.ts` header comment to document the service-client whitelist (per AC-20) and add a runtime assertion that throws if `getServiceClient()` is called outside whitelisted paths in dev.                                                                  | S      |
| 1.8 Verified Human badge component                        | **CREATE** `/Users/jyong/projects/world-shaker/components/world/VerifiedHumanBadge.tsx`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | S      |
| 1.9 Daily quota helper                                    | **CREATE** `/Users/jyong/projects/world-shaker/lib/quota/daily.ts` — reads `outcome_events` `viewed` count for `users.timezone`-local day. Returns `{ used, max, nextResetAt }`.                                                                                                                                                                                                                                                                                                                                                                                                                                                         | S      |
| 1.10 Rate-limit middleware                                | **CREATE** `/Users/jyong/projects/world-shaker/lib/auth/rate-limit.ts` — sliding-window rate limiter keyed by `world_user_id`, backed by Postgres (`rate_limit_buckets` table created in 0005) so it's serverless-safe. Default 30 req/min for `/api/agent/answer`.                                                                                                                                                                                                                                                                                                                                                                      | S      |
| 1.11 PostHog cohort hashing                               | **CREATE** `/Users/jyong/projects/world-shaker/lib/posthog/cohort.ts` — `hashCohort(world_user_id)` reads salt from `app_settings.posthog_cohort_salt`, returns sha256 hex. All `posthog.capture` calls in `lib/posthog/*` go through this helper. Quarterly rotation handled by `lib/inngest/functions/cohort-rotate.ts` (cron 1st of quarter 03:00 UTC).                                                                                                                                                                                                                                                                               | S      |
| 1.12 pgvector HNSW recall validation                      | **CREATE** `/Users/jyong/projects/world-shaker/scripts/validate-hnsw-recall.ts`. Inserts 1000 synthetic agent rows with random 1536-dim embeddings, queries top-10 via the partial HNSW index from `0001_initial.sql:105-107`, compares against brute-force linear scan, asserts recall ≥ 0.95. Runs in CI on Phase 1 completion.                                                                                                                                                                                                                                                                                                        | S      |

**Phase 1 deliverable**: schema migrations 0003-0006 applied (with rollback companion), RLS additions in place, conversations status state machine + transition trigger live, matches origin-aware partial UNIQUE in place, `match_candidates()` SQL fn callable, i18n round-trip works, badge ships, quota helper has unit tests, HNSW recall ≥0.95 verified, rate-limit middleware ready, PostHog cohort hashing live.

---

### Phase 2 — S-grade conversation core (LLM prompts + streaming)

**Goal**: the substance of the product. This is where 50%+ of the budget belongs. **KR-only** for the rubric pass; EN prompt rendering deferred to Phase 5.

| Step                               | Files (create / modify)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Effort |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --- |
| 2.1 Persona system prompt          | **CREATE** `/Users/jyong/projects/world-shaker/lib/llm/prompts/persona.ts`. Input: `extracted_features` + `language` (KR-only this phase via `BILINGUAL_PROMPTS_V1=false`). Output: a per-agent persona system prompt. Iterate against eval set of 10 hand-curated feature pairs.                                                                                                                                                                                                                                                                                                                                                                                                        | M      |
| 2.2 Agent-to-agent dialogue prompt | **CREATE** `/Users/jyong/projects/world-shaker/lib/llm/prompts/agent-dialogue.ts`. Drives turn N+1 given turn history, both personas, and stage cue (opening/probing/landing). Target 12-25 turns.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | L      |
| 2.3 Report extraction prompt       | **CREATE** `/Users/jyong/projects/world-shaker/lib/llm/prompts/report.ts`. Replaces/extends `/Users/jyong/projects/world-shaker/lib/llm/prompts/explanation.ts`. Input: full transcript. Output: `compatibility_score` (deterministic SQL `match_candidates()` still primary; LLM can refine via small bounded delta), `why_click`, `watch_out`, `highlight_quotes` (6-10), curated 30s-readable highlight summary.                                                                                                                                                                                                                                                                      | M      |
| 2.4 First-message starter prompt   | **CREATE** `/Users/jyong/projects/world-shaker/lib/llm/prompts/first-message.ts`. Pre-generated at `match.created` time and cached on `matches.starters` per AC-11.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | S      |
| 2.5 Probe prompt for interview     | **CREATE** `/Users/jyong/projects/world-shaker/lib/llm/prompts/interview-probe.ts`. Input: skeleton question + answer. Output: 1-2 follow-up probes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | S      |
| 2.6 Streaming gateway              | Modify `/Users/jyong/projects/world-shaker/lib/llm/openrouter.ts` to add `streamChat()` (returns `AsyncIterable<{delta, done}>`). Instrument every call with `posthog.capture('llm_cost', { tokens, cost_usd })` for AC-23 cost cap. Keep existing `generateExplanation`/`embedText`.                                                                                                                                                                                                                                                                                                                                                                                                    | M      |
| 2.7 Live conversation Inngest fn   | **CREATE** `/Users/jyong/projects/world-shaker/lib/inngest/functions/live-conversation.ts`. Steps: load both agents → `INSERT conversations (status='live')` → loop turns via `streamChat` → append each turn via `step.run` (idempotent) using `UPDATE conversations SET turns = turns                                                                                                                                                                                                                                                                                                                                                                                                  |        | $turn, last_turn_emitted_at = now() WHERE id = $id AND status = 'live'`(zero-rows-affected aborts). On completion:`UPDATE ... SET status = 'completed' WHERE status = 'live'`then call report fn. On failure (timeout, NSFW heuristic, repeat-loop detector in`/Users/jyong/projects/world-shaker/lib/llm/safety.ts`): `UPDATE ... SET status = 'failed' WHERE status = 'live'`and emit`conversation.failed`event. Honor`app_settings.streaming_paused`— abort early with`wont_connect` outcome event. | L   |
| 2.8 SSE relay route                | **CREATE** `/Users/jyong/projects/world-shaker/app/api/conversation/[id]/stream/route.ts` (Edge runtime). Uses `getServiceClient()` (whitelisted per AC-20) to read `conversations.turns`. Polls turn array length with backoff while `status='live'`. Emits SSE with `id: <turn_index>` per turn. Honors `Last-Event-ID` request header on reconnect — resumes from `turns[last_event_id + 1]`. Closes stream when `status` transitions to terminal state (`completed`/`abandoned`/`failed`). Authn: requires JWT, validates conversation participation by joining `conversations.agent_a_id` / `agent_b_id` to `agents.user_id = current_world_user_id()` server-side before relaying. | M      |
| 2.9 Failure heuristics             | **CREATE** `/Users/jyong/projects/world-shaker/lib/llm/safety.ts`: `detectRepeatLoop(turns)`, `detectHostileTone(turn)`, `detectNSFW(turn)` (regex + small classifier or moderation API).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | S      |
| 2.10 Report Inngest fn             | **CREATE** `/Users/jyong/projects/world-shaker/lib/inngest/functions/generate-report.ts`. Triggered by `conversation.completed`. Calls report prompt + `match_candidates()` for score baseline, writes match row with `origin` set, pre-generates `starters`, emits `match.created`.                                                                                                                                                                                                                                                                                                                                                                                                     | M      |
| 2.11 Abandon route                 | **CREATE** `/Users/jyong/projects/world-shaker/app/api/conversation/[id]/abandon/route.ts`. Server action. `UPDATE conversations SET status = 'abandoned' WHERE id = $id AND status = 'live'`; zero-rows-affected returns 409 (already terminal). Used when user explicitly closes conversation overlay.                                                                                                                                                                                                                                                                                                                                                                                 | S      |

**Phase 2 deliverable**: a CLI-driven smoke test where two seeded agents produce a complete streamed conversation + report (KR-only). Exit gate (concretized): rubric document at `/Users/jyong/projects/world-shaker/.omc/plans/phase2-rubric.md` with 10 dimensions (chemistry, distinct voices, conversational flow, callbacks, vulnerability, humor, language fidelity, pacing, ending strength, transcript readability), each scored 1-10. **Two reviewers** (PM + writer agent) score independently; final score = average. **Pass gate**: aggregate ≥ 7/10 with no single dimension < 5. **Fallback rule**: if aggregate is in `[5, 7)`, ship with `BILINGUAL_PROMPTS_V1=false` permanently for v0 (KR-only launch posture); if `< 5`, halt Phase 3 and re-run Phase 2 prompt iteration.

---

### Phase 3 — Loops (interview, stroll, match flow)

**Goal**: user-facing surfaces that wrap the S-grade core.

**Pre-condition**: OpenRouter image-model availability confirmed for AC-17. If not provisioned by Phase 3 kickoff, Step 3.4 ships placeholder pipeline only and `agents.avatar_url` defers to Phase 5.

| Step                                      | Files (create / modify)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Effort |
| ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 3.1 Verify + intro routes                 | **CREATE** `/Users/jyong/projects/world-shaker/app/(onboarding)/verify/page.tsx` (IDKit widget — uses `/api/rp-context` already in place), `/Users/jyong/projects/world-shaker/app/(onboarding)/intro/page.tsx` (30s video player; asset placeholder served from `/public/intro.mp4`).                                                                                                                                                                                                                                                                                                                                                                                                         | M      |
| 3.2 Interview UX                          | **CREATE** `/Users/jyong/projects/world-shaker/app/(onboarding)/interview/page.tsx` + `components/interview/QuestionCard.tsx`, `components/interview/ProbeBubble.tsx`. Resumable: query `agents.interview_answers` on mount, jump to first unanswered.                                                                                                                                                                                                                                                                                                                                                                                                                                         | L      |
| 3.3 Interview persistence + probe API     | **CREATE** `/Users/jyong/projects/world-shaker/app/api/agent/answer/route.ts` (next-safe-action; rate-limited via `lib/auth/rate-limit.ts` per AC-22). Persists answer; returns probe(s) inline using `interview-probe.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | M      |
| 3.4 Avatar generation                     | **CREATE** `/Users/jyong/projects/world-shaker/lib/avatar/generate.ts` (OpenRouter image model — pre-condition above; deterministic placeholder fallback keyed on hash of features). Wire as Inngest step in `firstEncounter`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | M      |
| 3.5 First-encounter Inngest fn + recovery | **CREATE** `/Users/jyong/projects/world-shaker/lib/inngest/functions/first-encounter.ts`. Triggered by `agent.activated`. Steps: pick seed/real candidate via `match_candidates()` (alpha-stage seed pool table; see Step 4.6) → spawn `live-conversation` → wait for completion → push notification (or in-app badge fallback). **Recovery path** (AC-21): `app/(app)/page.tsx` on mount calls `/api/first-encounter/recover` (next-safe-action) which checks for active agent + no conversation row, re-emits `agent.activated` exactly once (idempotency key = `agent_id`), and renders "preparing your first encounter" placeholder until `firstEncounter` fn writes the conversation row. | L      |
| 3.6 Conversation viewer                   | **CREATE** `/Users/jyong/projects/world-shaker/app/(app)/conversation/[id]/page.tsx` + `components/conversation/LiveTranscript.tsx` + `components/conversation/FailureOverlay.tsx`. Subscribes to SSE with `Last-Event-ID` resume support (AC-7b); renders turns; shows VerifiedHumanBadge. Handle leave/return. Close button POSTs to `/api/conversation/[id]/abandon`.                                                                                                                                                                                                                                                                                                                       | L      |
| 3.7 Match (report) viewer                 | **CREATE** `/Users/jyong/projects/world-shaker/app/(app)/match/[id]/page.tsx` + `components/match/HighlightCard.tsx` + `components/match/TranscriptToggle.tsx`. Default highlights view; expand toggle reveals full `rendered_transcript`.                                                                                                                                                                                                                                                                                                                                                                                                                                                     | M      |
| 3.8 Like / skip server action             | **CREATE** `/Users/jyong/projects/world-shaker/app/api/match/[id]/like/route.ts` (next-safe-action). Updates `matches.status` per AC-10; detects mutual via SQL on the **origin-aware partial UNIQUE**; emits `match.mutual` event.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | S      |
| 3.9 Mutual match success                  | **CREATE** `/Users/jyong/projects/world-shaker/app/(app)/match/[id]/success/page.tsx` + `components/match/StarterCard.tsx` + `components/match/AgentFarewell.tsx` (CSS-driven brief animation). Reads `matches.starters` (pre-cached). One-tap World Chat handoff via `world_chat_link`.                                                                                                                                                                                                                                                                                                                                                                                                       | M      |
| 3.10 Daily Stroll                         | **CREATE** `/Users/jyong/projects/world-shaker/app/(app)/stroll/page.tsx` + `components/stroll/CardStack.tsx`. Server-side: `match_candidates()` fan-out (random + incoming-like seeds) respecting daily quota and `app_settings.streaming_paused` cost-cap. Tap → spawn encounter (reuses Phase 2 fns).                                                                                                                                                                                                                                                                                                                                                                                       | M      |

**Phase 3 deliverable**: end-to-end happy path from Orb verify to mutual match works on dev MiniApp webview; KR renders correctly; SSE resume tested by manual reconnect; first-encounter recovery tested by killing the worker mid-spawn.

---

### Phase 4 — Operations (notifications, safety, quota, failure, seeds, cost cap)

**Goal**: the loop sustains itself across days and edge cases.

| Step                                | Files (create / modify)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Effort |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 4.1 Notification cadence            | **CREATE** `/Users/jyong/projects/world-shaker/lib/inngest/functions/daily-digest.ts` (cron 22:00 UTC, fans out per-user-locale morning push). Modify `/Users/jyong/projects/world-shaker/app/api/inngest/route.ts` to register. World App push: probe SDK capability at runtime; fallback to in-app badge surfaced via TanStack Query subscription on `/api/notifications/badge`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | M      |
| 4.2 Impact push                     | Hook into `match.mutual` event from Step 3.8. Same SDK probe as above.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | S      |
| 4.3 Safety menu                     | **CREATE** `/Users/jyong/projects/world-shaker/components/safety/SafetyMenu.tsx` (roll-up; lives in match/conversation/chat headers). **CREATE** `/Users/jyong/projects/world-shaker/app/api/report/route.ts` server action — writes to existing `reports` table; existing 2-strike trigger handles auto-suspend.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | M      |
| 4.4 Quota enforcement               | Wire `lib/quota/daily.ts` into `/Users/jyong/projects/world-shaker/app/(app)/stroll/page.tsx` and into `first-encounter`/encounter spawn paths. Pleasant "내일 다시" copy via i18n.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | S      |
| 4.5 Failure overlay wiring          | Connect `conversation.failed` event from Step 2.7 to client via SSE; `FailureOverlay` (Step 3.6) renders [재시작][닫기]; restart → spawn new conversation with same agents (new row, new id) and reset turn count; close → write `outcome_events` `wont_connect` and route home.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | M      |
| 4.6 Alpha seed pool                 | **CREATE** `/Users/jyong/projects/world-shaker/supabase/migrations/0007_seed_agents.sql` — inserts 12-20 seed agents (stylized personas, marked `is_seed=true` per Step 1.1). Modify `first-encounter` and `stroll` to mix seed agents until active user count >100.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | M      |
| 4.7 Outcome event coverage          | Audit every screen action; ensure `outcome_events` writes for `viewed`, `accepted`, `skipped`, `mutual`, `chat_opened`, `report_filed` per existing schema.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | S      |
| 4.8 Cost-cap kill switch (two-tier) | **CREATE** `/Users/jyong/projects/world-shaker/lib/inngest/functions/cost-cap.ts`. **Settlement cron** (00:01 UTC daily): aggregates the previous UTC day's `llm_cost` PostHog events via PostHog query API; if total > `app_settings.cost_cap_usd_daily`, sets `streaming_paused=true`; resets the flag if previous day was under cap (so a new day starts unpaused unless mid-day probe re-engages). **Mid-day probe cron** (every 4h: 04/08/12/16/20 UTC): aggregates the in-progress UTC day's cost events so far; if already over cap, sets `streaming_paused=true` defensively. Both paths share helper `aggregateLlmCost(start, end)`. Manual unpause via service-role row update. Surface "오늘은 산책이 모두 마감되었어요" on stroll surface and conversation spawn (AC-23). Reliability note: depends on PostHog ingestion freshness; honest worst case = the latest hour's events may be lagged, narrowing the practical guard window to ~3h instead of ~4h. | M      |
| 4.9 Cohort salt rotation            | **CREATE** `/Users/jyong/projects/world-shaker/lib/inngest/functions/cohort-rotate.ts` (cron 1st of each quarter, 03:00 UTC). Generates new 32-byte salt, updates `app_settings.posthog_cohort_salt` + `_rotated_at`. Backfills `users.posthog_cohort` via batched UPDATE. Emits `posthog_cohort_rotated` event for audit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | S      |

**Phase 4 deliverable**: loop runs unattended for 72h on staging with 5 test users + seed pool; no manual nudges required; failure overlay tested on injected faults; cost-cap dry-run with synthetic over-budget event seeds confirms streaming pause.

---

### Phase 5 — Polish + EN prompt rendering (avatar pipeline hardening, copy, PostHog, badges, EN rubric pass)

**Goal**: ship-ready. Everything from here strictly in service of the S-grade. Includes the EN prompt-rendering enablement and second rubric pass.

| Step                               | Files (create / modify)                                                                                                                                                                                                                                                                | Effort |
| ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 5.1 Avatar regeneration policy     | Decide one-shot vs. allow-rerender (default proposal: one-shot at v0; `agents.avatar_generated_at` immutable). Document in `/Users/jyong/projects/world-shaker/.omc/plans/open-questions.md`.                                                                                          | S      |
| 5.2 Verified Human placement audit | Confirm AC-18 across all screens; visual snapshot tests.                                                                                                                                                                                                                               | S      |
| 5.3 Copy pass (KR+EN)              | Walk through `lib/i18n/messages.ts`; tone = calm protector; pass through writer agent for both languages. Microcopy for quota, failure overlay, mutual match starters, daily digest push, cost-cap copy.                                                                               | M      |
| 5.4 PostHog event coverage         | Implement AC-19 events with cohort hashing; wire `lib/posthog/client.ts` + `lib/posthog/server.ts` calls; verify in PostHog Live View.                                                                                                                                                 | M      |
| 5.5 EN prompt rendering enablement | Flip `BILINGUAL_PROMPTS_V1=true` in staging. Re-run Phase 2 rubric in EN with same 2-reviewer protocol against 10 EN-localized feature pairs. **Launch gate**: same ≥ 7/10 aggregate with no dimension <5. If failure: keep flag false at launch (KR-only) and book a v1.1 EN re-eval. | M      |
| 5.6 Type generation refresh        | Run `npm run db:gen-types`; reconcile `/Users/jyong/projects/world-shaker/types/db.ts` with generated types.                                                                                                                                                                           | S      |
| 5.7 README + ENV updates           | Modify `/Users/jyong/projects/world-shaker/README.md` + `.env.example` with new env vars (image model, locale defaults, seed pool toggle, `COST_CAP_USD_DAILY`, `BILINGUAL_PROMPTS_V1`).                                                                                               | S      |

**Phase 5 deliverable**: production-launch-ready build; all ACs green; PostHog dashboard shows full funnel; EN rubric outcome documented and launch posture (KR-only vs. KR+EN) explicitly chosen.

---

## Phase Effort Summary

| Phase                  | Effort                | Critical path?                          |
| ---------------------- | --------------------- | --------------------------------------- |
| 1 — Foundation         | 6-8 person-days       | Yes (blocks 2/3)                        |
| 2 — S-grade core       | 9-12 person-days      | Yes (S-grade)                           |
| 3 — Loops              | 9-12 person-days      | Yes                                     |
| 4 — Operations         | 6-8 person-days       | Partially parallel to 3                 |
| 5 — Polish + EN rubric | 5-7 person-days       | No (launch gate at 5.5)                 |
| **Total**              | **35-47 person-days** | ~5-6 weeks elapsed with 1-2 specialists |

---

## Risks & Mitigations

| #   | Risk                                                                           | Likelihood     | Impact             | Mitigation                                                                                                                                                                                                                                                                                                                                                    |
| --- | ------------------------------------------------------------------------------ | -------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | -------------------------------------------------------------------------------------------------------- |
| R1  | **LLM cost spike from streaming + Inngest retries.**                           | High           | High               | Cap turns at 25 with hard stop; cache persona prompts per agent; `step.run` idempotency keys; instrument `llm_cost` PostHog event in `lib/llm/openrouter.ts`; **Phase 4.8 cost-cap kill switch (AC-23)** auto-pauses streaming when daily aggregate exceeds `app_settings.cost_cap_usd_daily`.                                                                |
| R2  | **Persona collapse / both agents sound identical.**                            | High           | Critical (S-grade) | Phase 2 exit gate is 2-reviewer rubric (AC + writer agent) against 10 dimensions; iterate persona + dialogue prompts until aggregate ≥7/10 with no single dimension <5; use `extracted_features` divergence as input variety; consider giving each agent a private "voice card" stored in `extracted_features.voice` that is sampled deterministically.       |
| R3  | **World App push capability uncertain.**                                       | Medium         | Medium             | All push paths are SDK-probed and fall back to in-app badge; daily digest is opportunistic, not load-bearing for first-encounter wow (which is in-session).                                                                                                                                                                                                   |
| R4  | **Alpha-stage empty-pool problem.**                                            | High at launch | High               | Step 4.6 ships 12-20 seed agents; mix ratio 100% seeds at <10 active users, fading linearly to 0% at 100+; flag-controlled.                                                                                                                                                                                                                                   |
| R5  | **Streaming SSE reliability on Worldcoin MiniApp webview.**                    | Medium         | High               | **AC-7b implements concrete resume protocol**: server emits `id: <turn_index>` per SSE event; client `Last-Event-ID` header on reconnect; route resumes from `turns[Last-Event-ID + 1]`. Combined with the `conversations.status` state machine, dropped clients reattach without restart and final report is the durable surface.                            |
| R6  | **NSFW / hostile content slips through and hurts trust.**                      | Medium         | Critical           | Step 2.9 heuristics + OpenRouter moderation pre-filter on each generated turn (drop turn → trigger failure path before rendering to client).                                                                                                                                                                                                                  |
| R7  | **Bilingual prompt drift — EN output sneaks into KR session or vice versa.**   | Medium         | Medium             | **Sequenced**: KR-only in Phase 2 (`BILINGUAL_PROMPTS_V1=false`), single-language pool by default, language-stickiness check in rubric. EN enabled in Phase 5.5 with second rubric pass before launch flip. Cross-language pairs deferred to v1+.                                                                                                             |
| R8  | **Avatar generation latency exceeds 60s budget.**                              | Medium         | Low                | Async with placeholder avatar shown immediately; real avatar swaps in via TanStack Query refetch; quality not S-grade so degraded experience acceptable. **Image-model availability is a Phase 3 blocking pre-condition** — if no model provisioned, ship placeholder pipeline only and document gap.                                                         |
| R9  | **Schema migration breaks existing seed data / dev DB.**                       | Low            | Medium             | All Phase 1 changes additive (`ALTER TABLE ... ADD COLUMN ... DEFAULT`); `conversations.status` backfilled as `completed` for existing rows then default flipped to `live`; matches partial UNIQUE drop+recreate documented; `0003_rollback.sql` companion ships at the same time (Step 1.5); RLS additions in separate file; tested on local Supabase first. |
| R10 | **Track B "Daily Stroll cards" feels too far from the spec's Pokemon vision.** | Medium         | Medium (product)   | Document tension; ship card stack with subtle parallax + ambient motion to gesture toward "stroll"; full Sims-style isometric world is `STROLL_WORLD_V1` flag for v1+.                                                                                                                                                                                        |
| R11 | **First-encounter pipeline orphans on user dropoff.**                          | Medium         | High (Day-1 wow)   | **AC-21 recovery path**: server detects active-agent + no-conversation on home load and re-emits `agent.activated` with idempotency key.                                                                                                                                                                                                                      |
| R12 | **PostHog distinct_id leaks raw `world_user_id` (Worldcoin privacy risk).**    | High pre-fix   | High               | **AC-19 cohort hashing**: `users.posthog_cohort = sha256(world_user_id                                                                                                                                                                                                                                                                                        |     | app_settings.posthog_cohort_salt)`, salt rotated quarterly via Step 4.9. Raw UUID never sent to PostHog. |
| R13 | **Chat-flood DoS during interview probe.**                                     | Medium         | Medium             | **AC-22 rate-limit middleware** in `lib/auth/rate-limit.ts`; 30 req/min per `world_user_id` on `/api/agent/answer`; HTTP 429 + PostHog `rate_limit_hit` event.                                                                                                                                                                                                |
| R14 | **HNSW recall degrades silently after schema additions.**                      | Low            | Medium             | Step 1.12 validation script asserts recall ≥0.95 in CI on Phase 1 completion; re-run after any schema change touching `agents`.                                                                                                                                                                                                                               |

---

## Verification Steps (per phase)

1. **Phase 1**: `npm run typecheck` clean; migrations 0003-0006 apply on local Supabase; rollback companion 0003 dry-runs cleanly on a snapshot; RLS test suite (new file `/Users/jyong/projects/world-shaker/__tests__/rls.test.ts`) passes for `language_pref`, `avatar_url`, `growth_log` ownership and confirms `conversations` remains client-blocked; `match_candidates()` unit test passes; HNSW recall script passes ≥0.95.
2. **Phase 2**: Smoke-test CLI script `/Users/jyong/projects/world-shaker/scripts/smoke-conversation.ts` runs 10 seeded encounter pairs (KR-only), dumps transcripts to `/tmp/`; **2 reviewers** independently score against the rubric at `/Users/jyong/projects/world-shaker/.omc/plans/phase2-rubric.md` (10 dimensions, 1-10 scale); gate aggregate ≥7/10 with no dim <5; SSE resume integration test passes; abandon route returns 409 on terminal-state attempt.
3. **Phase 3**: Playwright suite `/Users/jyong/projects/world-shaker/e2e/onboarding-to-mutual.spec.ts` covers Orb stub → interview → first encounter → report → like → mutual → World Chat link click. Run in KR. First-encounter recovery test: kill worker mid-spawn → reload home → assert recovery emission + placeholder.
4. **Phase 4**: 72h staging run with 5 internal testers + seed pool; daily-digest cron fires; failure-injection harness `/Users/jyong/projects/world-shaker/scripts/inject-fault.ts` triggers each failure mode → overlay verified; cost-cap dry-run with synthetic over-budget seed sets `streaming_paused=true` and surfaces user-facing copy; rate-limit triggers 429 at 31st req/min.
5. **Phase 5**: PostHog dashboard `world-shaker-funnel-v0` shows full funnel for last 24h with hashed `distinct_id`s; all AC checklists green; visual diff snapshots stable; lint + typecheck + tests all green; README/env updated; **EN rubric pass** result recorded; launch posture (KR-only vs. KR+EN) explicitly chosen and documented.
6. **Final ship gate**: `oh-my-claudecode:verify` invocation against the original 19 spec acceptance criteria + 23 plan ACs; reviewer evidence collected; `oh-my-claudecode:code-reviewer` (opus) review of `lib/llm/prompts/*`, `lib/inngest/functions/live-conversation.ts`, `app/api/conversation/[id]/stream/route.ts`, and migrations 0003-0006.

---

## Pre-mortem (since rubric + cost-cap touch high-risk surfaces; abbreviated)

1. **"We shipped on time but the conversations feel flat."** Most likely path: Phase 2 budget compressed to <40%, rubric pass was a single rater. Mitigation: 50%+ budget locked to Phase 2; 2-reviewer rubric protocol; fallback rule documented.
2. **"Cost-cap fired mid-day on launch and users saw the closed-park copy for 18 hours."** Most likely path: cap set too low. Mitigation: `app_settings.cost_cap_usd_daily` defaults conservative, monitored in PostHog `llm_cost` dashboard; manual override is a single service-role UPDATE.
3. **"PostHog audit flags distinct_id pattern as PII."** Most likely path: salt rotation never ran, hash is reversible across data exports. Mitigation: quarterly rotation via Step 4.9, audit log entry `posthog_cohort_rotated`, rotation date surfaced on internal /admin page (v1).

---

## Open Tensions Surfaced for Architect/Critic Review

1. **Track B fidelity downgrade.** v0 ships "Daily Stroll cards" instead of Sims-style isometric world. Spec explicitly says v0 may approximate, but product owner alignment recommended before Phase 3.
2. **One-shot avatar policy.** Recommend immutable; spec leaves it as designer judgment. Could ship as "regen-once-allowed" with minor schema cost.
3. **Cross-language matching.** v0 defaults to same-language pool; opt-in cross-language is spec-deferred. Recommend not implementing in v0 to protect prompt quality.
4. **Cost-cap default.** Set conservatively at $50/day in `app_settings`; revisit after Phase 2 cost-per-encounter is measured.
5. **EN launch posture.** Conditional on Phase 5.5 rubric; KR-only fallback documented if EN fails.

---

## ADR (Architecture Decision Record)

**Decision**: Build v0 MVP per Option A — Track A end-to-end with Track B as Daily Stroll cards stub, **bilingual schema + i18n shell in Phase 1 with EN prompt rendering deferred to Phase 5 behind `BILINGUAL_PROMPTS_V1` flag**, S-grade investment concentrated in Phase 2 (LLM prompts + streaming infra) with 2-reviewer rubric gate.

**Drivers**:

1. Conversation chemistry is S-grade; everything else is B-grade-OK (spec R9).
2. 4-6 week budget cannot ship dual-track at full polish.
3. Re-engineering bilingual schema post-launch costs more than wiring the shell now; sequencing EN prompt rendering to Phase 5 protects Phase 2 rubric focus without forfeiting bilingual launch posture.

**Alternatives Considered**:

- Option B (both tracks half-quality): rejected — erodes S-grade.
- Option C (KR-only entirely, bilingual deferred): rejected — contradicts R15; re-engineering schema cost higher than upfront. Synthesized variant (KR-only schema + EN entirely v1+) rejected as too far from spec; instead the Critic-suggested compromise is adopted.
- Origin-blind matches UNIQUE: rejected — blocks dual-track pending matches; revised to `(user_id, candidate_user_id, origin)`.
- RLS policy on `conversations` for client SELECT: rejected — opens read surface; instead added `/api/conversation/[id]/stream` to `getServiceClient()` whitelist.

**Why Chosen**: Maximum S-grade preservation within budget; minimum re-engineering risk; preserves all six core schema tables and RLS contract from `0001_initial.sql`/`0002_rls.sql` (additive only); explicit launch-gate on EN prompts so a failed rubric does not hold the launch.

**Consequences**:

- Track B Pokemon-encounter world deferred to v1+. Product owner sees a card stack, not a world, on Day 1.
- All LLM prompts must accept `language` from day 1 — slight prompt-engineering overhead.
- Streaming infra is now a load-bearing dependency (can't be downgraded without erasing the live-rendering acceptance criterion).
- Two distinct rubric passes (KR in Phase 2, EN in Phase 5.5) — 2 person-days of additional human-eval cost.
- Cost-cap kill switch is now infrastructure: requires PostHog query API access from Inngest; an outage in PostHog could disable the kill switch (acceptable — soft cap, not hard cap).
- `matches.origin`-aware partial UNIQUE permits one match per origin track per pair — explicit design choice; reports surfaced to user are filtered by origin in `/api/match/...`.

**Follow-ups**:

- Schedule v1 Sims-world track 2-3 weeks post-launch.
- Cross-language matching design pass at v1.
- WLD payments + Agora ritual remain v2 per existing roadmap.
- Avatar regeneration policy revisit at v1 if user complaints surface.
- EN rubric re-eval at v1.1 if Phase 5.5 fails.
- Cost-cap to be replaced with hard rate-limit at LLM gateway in v1.

---

## Plan Confirmation

**Does this plan capture the v0 MVP intent?**

- "proceed" → hand off to `oh-my-claudecode:start-work world-shaker-ux-v1`
- "adjust [X]" → return to interview to modify
- "restart" → discard and start fresh

---

## Changelog

### v2 (iteration 2/5, 2026-04-26)

**CRITICAL fixes:**

- Added Step 1.4 `0006_compatibility_score.sql` and AC-8b for `match_candidates()` SQL function (was entirely absent in v1).
- Added Step 2.8 documentation on `getServiceClient()` whitelist for `/api/conversation/[id]/stream`; updated AC-20 whitelist; rationale captured in ADR.
- Added `conversations.status` state machine (live/completed/abandoned/failed) in Step 1.1 with explicit backfill clause (`DEFAULT 'completed'` for existing rows then flip to `'live'`), `BEFORE UPDATE` transition guard trigger, and `WHERE status = 'live'` predicate on every Inngest turn append (AC-6, Step 2.7).
- Revised matches partial UNIQUE to `(user_id, candidate_user_id, origin) WHERE status IN ('pending','accepted','mutual')` to support dual-track origins (Step 1.1, AC-10).
- Added `/api/conversation/[id]/abandon` route (Step 2.11).

**MAJOR fixes:**

- Implemented SSE resume protocol: AC-7b, `conversations.last_turn_emitted_at` column, `id:` event ids, `Last-Event-ID` reconnect (Step 2.8).
- Replaced raw `world_user_id` PostHog distinct_id with `users.posthog_cohort = sha256(world_user_id || salt)`; added `app_settings.posthog_cohort_salt` (Step 1.3) and quarterly rotation Inngest fn (Step 4.9, AC-19, R12).
- Added AC-21 first-encounter pipeline recovery (Step 3.5, R11).
- Concretized Phase 2 rubric gate: `phase2-rubric.md`, 2-reviewer protocol, 10 dimensions × 1-10 scale, ≥7/10 aggregate gate with no dim <5, fallback rule (KR-only ship at [5,7), halt-and-iterate at <5).
- Synthesized bilingual scope per Architect: schema/shell in Phase 1, KR-only Phase 2 rubric, EN prompt rendering in Phase 5 behind `BILINGUAL_PROMPTS_V1` flag with second rubric pass (Phase 5.5).

**MINOR fixes:**

- Added `users.timezone` and `agents.is_seed` to Step 1.1 migration (referenced but missing in v1).
- Made OpenRouter image-model availability a Phase 3 blocking pre-condition (AC-17, Phase 3 header).

**Architect-stated additions (re-affirmed):**

- All `conversations` writes in Inngest path scoped by `WHERE status = 'live'`.
- Migration backfill ordering documented explicitly.

**Proactive additions per Critic:**

- AC-22 + Step 1.10 rate-limit middleware on `/api/agent/answer`.
- AC-23 + Step 4.8 cost-cap kill switch (Inngest cron + `app_settings.streaming_paused`).
- Step 1.12 HNSW recall validation script (≥0.95 threshold).
- Step 1.5 `0003_rollback.sql` companion with manual-recovery-only header.

**Other**: phase totals updated (Phase 1 6-8 days, Phase 4 6-8 days, Phase 5 5-7 days, total 35-47 person-days). Risks expanded R11-R14. ADR alternatives expanded with rejection rationale for origin-blind UNIQUE and conversations RLS-policy variant. Pre-mortem section added (3 scenarios) at Critic's prompting since cost-cap and rubric introduce new failure surfaces.

**Partially addressed (honest accounting):**

- Phase 2 rubric "single rater is bias risk" mitigated by 2-reviewer protocol (PM + writer agent), but a true 3rd-party rater rotation is deferred to v1 once budget allows. Documented.
- AC-23 cost-cap depends on PostHog query API as the spend source-of-truth; an LLM gateway-side hard rate limit is more robust and listed in ADR follow-ups for v1.
- HNSW recall is verified at Phase 1 only; no continuous re-validation as data scales. Listed in R14 mitigation.

### v2.1 (cheap improvements merged inline before ADR finalize, 2026-04-26)

Per Architect APPROVE + Critic APPROVE-WITH-RESERVATIONS at 0.83 confidence, 4 sub-1-hour fixes were merged in v2.1 rather than triggering iteration 3:

- **AC-8b (Architect #3)**: `match_candidates()` exclusion semantics now explicit in SQL body — excludes self (`candidate.user_id <> target_user`), inactive agents, and already-pending/accepted/mutual matches. Calibration TODO documented in migration header.
- **AC-19 (Critic D1)**: PostHog cohort rotation now writes `posthog_cohort_predecessor` person property, bridging retention/funnel dashboards across quarterly salt rotation boundaries.
- **AC-20 (Architect #1)**: Service-client whitelist now backed by CI grep test at `tests/ci/service-client-whitelist.test.ts` + mirrored in `.omc/plans/service-client-allowlist.txt`. Whitelist drift fails the build.
- **AC-23 + Step 4.8 (Architect #2)**: Cost-cap promoted to two-tier — daily 00:01 UTC settlement + 4-hourly mid-day probe (04/08/12/16/20 UTC). Worst-case burn window narrowed from ~24h to ~3-4h depending on PostHog ingestion lag.

Final consensus posture: APPROVED at confidence ~0.85.
